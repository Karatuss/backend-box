plugins {
    id 'java-library'
}

description = 'Make backend environment comfortably for Spring and Spring Boot'

sourceCompatibility = JavaVersion.VERSION_17
targetCompatibility = JavaVersion.VERSION_17

ext {
    url = 'https://github.com/Karatuss/backend-box'
}

jar {
    enabled = true
}

java {
    withJavadocJar()
    withSourcesJar()
}

dependencies {

    // test
    // TODO: 버전 확인하고 맞춰주기
    testImplementation 'org.junit.jupiter:junit-jupiter-api:5.9.3'
    testRuntimeOnly 'org.junit.jupiter:junit-jupiter-engine:5.9.3'
    testImplementation 'org.mockito:mockito-core:5.3.1'
    testImplementation 'org.assertj:assertj-core:3.24.2'
}

test {
    useJUnitPlatform()
//    environment 'EXAMPLE_MESSAGE_2', 'Message 2 from system environment'
//    environment 'EXAMPLE_MESSAGE_3', 'Message 3 from system environment'
}

apply from: rootProject.file('gradle/publish-to-ossrh.gradle')
